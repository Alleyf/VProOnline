# VProOnline 环境变量配置

# Vercel Blob Store 配置
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxx
BLOB_STORAGE_ENABLED=true
BLOB_CLEANUP_LOCAL=true

# 服务器配置
PORT=3001
PUBLIC_URL=http://localhost:3001

# 上传验证配置
# 设置一个强密码作为上传验证密钥
UPLOAD_AUTH_TOKEN=change_this_to_a_secure_random_string_in_production
# Token有效期（毫秒）- 默认1小时
UPLOAD_TOKEN_EXPIRY=3600000
# 是否启用上传验证 - 生产环境建议设为true
UPLOAD_REQUIRE_AUTH=true

# 注意事项：
# 1. BLOB_READ_WRITE_TOKEN: 你的 Vercel Blob Store 访问令牌
# 2. BLOB_STORAGE_ENABLED: 设置为 true 启用 Blob 存储，false 使用本地存储
# 3. UPLOAD_AUTH_TOKEN: 强烈建议在生产环境中更改为复杂的随机字符串
# 4. 在生产环境中，请确保将这些敏感信息设置为环境变量而不是硬编码